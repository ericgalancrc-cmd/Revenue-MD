services:
  - type: web
    name: revenuemd-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: revenuemd-db
          property: connectionString
      - key: CORS_ORIGINS
        value: https://revenue-md.vercel.app  # update to your real Vercel URL
      - key: AUTH0_DOMAIN
        value: dev-mvhfi6mnltzukbs2.us.auth0.com
      - key: AUTH0_AUDIENCE
        value: https://api.revenuemd.com

databases:
  - name: revenuemd-db
    plan: free
    databaseName: revenuemd
