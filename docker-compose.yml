version: "3.9"

services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app        # live-reload
    command: uvicorn main:app --reload --host 0.0.0.0 --port 8000
    env_file:
      - ./backend/.env
    restart: unless-stopped
